name: CI
on: [push, pull_request]

permissions:
  contents: read

jobs:
  ci:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    uses: loonghao/rust-actions-toolkit/.github/workflows/reusable-ci.yml@v4.0.0
    with:
      rust-toolchain: stable
